(jbuild_version 1)

(executable
  ((name mancala)
   (modules (common ai board mancala))
   (libraries (containers))))

;(executable
;  ((name test)
;   (modules (common ai board test))
;   (libraries (containers oUnit))))
